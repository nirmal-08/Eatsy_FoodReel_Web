import React from 'react';

// Placeholder Saved page; integrate real saved items logic later
const Saved = () => {
  return (
    <div className="saved-page">
      <h2>Saved Items</h2>
      <p style={{ opacity: 0.75 }}>You haven't saved anything yet.</p>
    </div>
  );
};

export default Saved;
